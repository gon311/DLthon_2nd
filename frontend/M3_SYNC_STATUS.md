# M3 최신 연동 상태

업데이트 기준일: 2026-08-10

## 연동 경로

- 개인 구현: `jsk900210-oss/jeju-bucket-map`의 `gpt`
- 포크 작업: `jsk900210-oss/DLthon_2nd`의 `m3`
- 팀 검토: `gon311/DLthon_2nd`의 PR #7 (초안 해제 완료, ready for review)
- 운영 웹: https://bucket-jeju-join.ep01-sleepwar.chatgpt.site/

팀 저장소에는 `m3` 브랜치가 없고 현재 사용자에게 직접 쓰기 권한도 없습니다.
따라서 포크 `m3`를 최신화하고 PR #7로 팀 `M3`에 제안하는 흐름을 사용합니다.

## 이번에 반영한 내용 (2026-08-10 동기화)

개인 `gpt` 브랜치(운영 웹데모)의 최신 프론트엔드를 포크 `m3`에 반영했습니다.

- 근처 발견 지도에 버킷제주 중심 반경 2km 정보 제공 경계(원형 가이드) 표시
- 지도 마커 좌표를 실제 POI(kakao_local) 실좌표로 검수·교체 — 바다 위 오표기 제거
- 근처 플레이스 카테고리·이모지 마커/필터(식당🍽️·편의점🏪·해변🏖️·관광📷·주차장🅿️)
- 검수된 2km 범위 확정(반경 5km 실험은 되돌림, 송악산·선착장 등 2km 밖 지점 제외)
- 장소 클릭 시 상세 안내 패널
- Join 모집중·모집완료·일정완료 3상태 분리와 지난 일정 별도 표시
- 반복(주간) 프로그램 상단 고정
- 게스트하우스 FAQ 데이터(`seed-data/guesthouse-faq.ts`)와 다국어 카피 연동
- Join 참여자 API 경로(`app/api/joins/[id]/participants`) 추가

## 이전에 반영한 내용 (2026-08-04)

- ChatGPT 로그인 사용자와 D1 사용자 레코드 연결
- 닉네임 2~20자 변경 및 영구 저장
- Join 목록 `GET /api/joins`, 작성 `POST /api/joins`
- Join 작성 폼과 입력 검증, 작성 성공 직후 Join 탭 반영
- 최신 Join 3건을 메인 화면에 반영
- 모집시간이 지나면 인원과 무관하게 `모집완료`로 자동 전환
- AI 질문 탭 기본 화면 추가
- Drizzle 사용자·Join·참여자 스키마 및 마이그레이션 추가

## 아직 해야 할 일

- Join 참여·취소를 D1에 영구 저장
- RAG 자료 수집 후 AI 질문 기능 연결
- 게스트하우스 1층·2층 안내, 공간별 이용시간과 규칙 정리(숙소 정보 JSON 반영)
- 방문객 리뷰 탭 구현(방문 인증)
- 하드코딩된 POI(21곳)를 데이터 파이프라인/DB 기반으로 전환
- 팀 리뷰 후 PR #7 병합(팀 관리자 권한 필요)

## 검증 메모

- 최신 기능 소스는 개인 `gpt` 브랜치(운영 웹데모)에서 확인했습니다.
- 지도 좌표는 `data/processed/guesthouse_pois.csv`의 kakao 실좌표 기준으로 검수했습니다.
- 2km 지도·마커는 운영 웹데모에서 육안 검수를 마쳤습니다.
